# TiKV PCR 源码修改清单

**日期**: 2026-04-27
**编译环境**: macOS 26.4.1, Rust nightly-2023-12-28, protoc 21.12, Apple Clang 21 (CXX wrapper)

---

## 新建文件（25 个）

### `components/stream-ingest/` — PCR Consumer crate（18 个）

| # | 文件 | 说明 |
|---|------|------|
| 1 | `Cargo.toml` | crate 定义，依赖 engine_traits/kvproto/grpcio 等 |
| 2 | `build.rs` | protoc 检测编译脚本 |
| 3 | `src/lib.rs` | 公共接口 + `create_stream_ingest_task()` 工厂函数 |
| 4 | `src/sst_batcher.rs` | **核心**：KV 缓冲→Key 排序→SST 生成→DirectIngest |
| 5 | `src/direct_ingest.rs` | **关键**：绕过 Raft，`ingest_external_file_cf` 写入 Tablet |
| 6 | `src/task.rs` | StreamIngestTask 主控 + `Runnable` worker + 事件循环 |
| 7 | `src/subscriber.rs` | gRPC 多分区流订阅 + 自动重连 |
| 8 | `src/checkpoint.rs` | PD 安全点 checkpoint 管理 |
| 9 | `src/config.rs` | `StreamIngestConfigManager`（动态热更新） |
| 10 | `src/errors.rs` | 9 种错误类型 |
| 11 | `src/metrics.rs` | 8 个 Prometheus 指标 |
| 12 | `src/pcrpb_gen/mod.rs` | PCR protobuf 生成代码模块 |
| 13 | `src/pcrpb_gen/pcrpb.rs` | 9 个 protobuf 消息类型 + `impl_message_stub!` 宏 |
| 14 | `src/pcrpb_gen/pcrpb_grpc.rs` | gRPC PcrStreamClient stub |
| 15 | `tests/unit_tests.rs` | 48 单元测试（SstBatcher/Checkpoint/Dispatcher/MVCC编码） |
| 16 | `tests/pcrpb_tests.rs` | 12 类型测试 |
| 17 | `tests/integration_tests.rs` | 集成测试桩 |
| 18 | `PCR_指南.md` | 438 行配置与使用手册 |

### `components/cdc/src/` — CDC Producer 扩展（3 个）

| # | 文件 | 说明 |
|---|------|------|
| 19 | `pcr_event_batcher.rs` | PCR 事件批处理（含 22 tests），对标 CRDB `streamEventBatcher` |
| 20 | `pcr_service.rs` | PcrStream gRPC 服务端 |
| 21 | `pcr_types.rs` | PCR 事件类型桩（kvproto pcrpb 发布后替换） |

### `proto/pcrpb/` — 协议定义（1 个）

| # | 文件 | 说明 |
|---|------|------|
| 22 | `pcrpb.proto` | PcrStream gRPC 服务 + 6 种事件消息 |

### `cmd/pcr-ctl/` — CLI 管理工具（2 个）

| # | 文件 | 说明 |
|---|------|------|
| 23 | `Cargo.toml` | 轻量依赖（clap/tokio/serde） |
| 24 | `src/main.rs` | 7 个子命令（start/status/pause/resume/cutover/list/delete） |

### 文档（1 个）

| # | 文件 | 说明 |
|---|------|------|
| 25 | `develop/修改清单.md` | 本文件 |

---

## 修改已有文件（10 个）

| # | 文件 | 改动说明 |
|---|------|---------|
| 1 | `Cargo.toml`（根） | +workspace member `"components/stream-ingest"`, +workspace dependency `stream-ingest` |
| 2 | `components/server/Cargo.toml` | +`stream-ingest = { workspace = true }` |
| 3 | `components/cdc/Cargo.toml` | +`sst_importer = { workspace = true }` |
| 4 | `components/cdc/src/lib.rs` | +`mod pcr_event_batcher`, `mod pcr_types`, `pub mod pcr_service`, +pub export |
| 5 | `components/cdc/src/delegate.rs` | +`sst_importer: Option<Arc<SstImporter>>` 字段, +`pcr_batcher`/`pcr_event_sink` 字段, `sink_data()` 新增 `CmdType::IngestSst`/`Delete` 分支, `sink_put()` 双写 PCR batcher, +`enable_pcr()`/`emit_pcr_event()`/`set_sst_importer()` 方法 |
| 6 | `components/cdc/src/endpoint.rs` | +`sst_importer: Option<Arc<SstImporter>>` 字段, `Task` 新增 `StartPcrStream` 变体, +`set_sst_importer()`/`on_start_pcr_stream()` 方法, Debug impl +StartPcrStream arm, run() 新增 StartPcrStream dispatch |
| 7 | `components/server/src/server2.rs` | +`stream_ingest_scheduler` 字段, init_servers 条件初始化 StreamIngestTask, register_services 注册 PcrService gRPC, SstImporter 前置注入 CDC Endpoint |
| 8 | `components/server/src/server.rs` | 同上（v1 raftstore 兼容模式，额外检查 `tablet_registry.is_some()`） |
| 9 | `src/config/mod.rs` | +`StreamIngestConfig` struct（含 Default/validate）, +`Module::StreamIngest` 枚举, +`From<&str>` 映射 ×2, `TikvConfig` +`stream_ingest` 字段, 挂入全局 `validate()` |
| 10 | `src/storage/config.rs` | 文档注释补充 |

---

## 编译输出

| 文件 | 大小 | 类型 |
|------|------|------|
| `target/debug/tikv-server` | 413 MB | Mach-O 64-bit arm64 |

## 外部依赖（未修改，仅引用）

- `components/raftstore-v2/` — `TabletRegistry`, `StoreMeta`
- `components/sst_importer/` — `SstImporter`, `SstMetaInfo`
- `components/backup-stream/` — 参考 `Endpoint`, `CheckpointManager` 架构模式
- `components/resolved_ts/` — Producer 端 resolved_ts 复用
- `components/pd_client/` — PD 通信
- `components/engine_traits/` — `KvEngine`, `ImportExt`, `SstExt`
- `components/engine_rocks/` — `RocksEngine`, `RocksSstWriter`
- `kvproto` — 现有 protobuf 类型引用（pcrpb 后续独立发布）
