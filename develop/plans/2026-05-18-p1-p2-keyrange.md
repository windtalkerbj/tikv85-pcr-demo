# P1-P2: Sink Intrinsic + Partial Span Support

## #2: auto_match_pcr 传实际 region key range

**Problem:** auto_match_pcr 用空 key `match_region(0, &[], &[])`, 只能在 full-span 场景工作

**Fix:** 从 delegate 出生路径提取 region 实际 start/end key，传入 match_region

**Files:** endpoint.rs

### Steps:
- [ ] Step 1: on_start_pcr_stream — 从 PD get_region_by_id 结果中提取 start_key/end_key，传给 auto_match_pcr
- [ ] Step 2: on_register — 从 StoreMeta regions 中提取 key range，传给 auto_match_pcr
- [ ] Step 3: 更新 auto_match_pcr 签名接受 key range 参数
- [ ] Step 4: Build + verify

## #3: 回归测试

- [ ] Step 1: 重启集群 + INSERT/DELETE 收敛测试
