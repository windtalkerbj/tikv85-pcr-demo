# 2026-05-07 思索过程

## mem0 集成

- 用户安装了 mem0 插件（`/plugin install mem0@mem0-plugins`），但 MCP 工具一直不可用
- 最终通过 Python SDK 直连方式 work（`pip install mem0ai --break-system-packages`）
- 已存入 8 条 PCR 关键记忆，检索验证通过
- API key 配置在 `.claude/settings.json` 的 `env.MEM0_API_KEY`

## 文档整理

- memory/ 目录 12 个文件 → 清理到 3 个（MEMORY.md, user-chinese.md, feedback-testing.md）
- 研究文档合并为 `develop/pcr-research.md`（CRDB架构 + TiKV可行性 + TiCDC对比）
- 完整简报 `develop/daily-briefing.md` 已涵盖全时间线

## 新规则

1. tikv-server 编译后视情况清数据（不清配置文件），其他组件无要求
2. 思索过程保存到 `develop/thinking/` 每日一个 MD

## 当前状态

- PCR bounded scan 方案已确认（lower_bound + 代码级 to-check）
- 多 Region delta 验证：3 个 bounded Region 各自找到增量
- 配置持久化到 develop/pcr-configs/，不再丢
- tiup launchd 服务需要手动 unload（10+ 服务抢端口）
